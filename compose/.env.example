# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

# Password for the Splunk admin user (also used by the universal forwarder
# to authenticate). Must meet Splunk's minimum password requirements.
SPLUNK_PASSWORD=changeme
